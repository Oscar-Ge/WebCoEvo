# First Modified Linkding Website

This folder stores the first modified Linkding drift website generation, before hardv3.

## Contents

- `variant_templates/`: template overrides restored from old `webAgentBenchmark` commit `9879707` (`feat: add Linkding drift runtime, tasks, and M3 reports`).
- `report/README.md`: the April 3 before/after report.
- `report/assets/*.png`: before/after screenshots for `access`, `content`, `functional`, `process`, `runtime`, `structural`, and `surface`.

This generation is useful historical evidence, but it is not the current benchmark runtime. WebCoEvo evaluates the release-grounded hardv3 assets unless a caller explicitly rewires the manifest.
